# The installed agent skill library. It is a toolchain, not part of this project.
.agents/

# Dependencies
node_modules/

# Build output: regenerated by `npm run build`, and the artifact the tests load.
lib/
